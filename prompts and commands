python manage.py runserver

.\venv\Scripts\activate

deactivate

python manage.py makemigrations
python manage.py migrate

Prevodjenje:

# Only Croatian translation needed - source strings are in English
python manage.py makemessages -l hr --ignore=venv --ignore=env --ignore=.venv
python manage.py compilemessages


Git commit and push commands(puštati jednu po jednu):

git add .
git commit -m "update"
git push

git push --force - nakon backup kopiranja

